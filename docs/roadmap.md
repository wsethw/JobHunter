# Roadmap

- Add more job sources with stable public APIs.
- Add a small web dashboard frontend on top of the FastAPI API.
- Add Prometheus metrics export.
- Add robots.txt enforcement per domain.
- Add source-specific rate limit policies.
- Add semantic matching with embeddings for richer job compatibility.
